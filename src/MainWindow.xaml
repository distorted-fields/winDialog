<Window x:Class="winDialog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="winDialog" Height="400" Width="650" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    
    <Border CornerRadius="12" Background="#222222" BorderBrush="#333333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/> <RowDefinition Height="*"/>   </Grid.RowDefinitions>

            <Border Grid.Row="0" CornerRadius="11,11,0,0">
                <Border.Background>
                    <ImageBrush x:Name="BannerBrush" Stretch="UniformToFill" />
                </Border.Background>
            </Border>

            <Grid Grid.Row="1" Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

                <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,15" Text="Title"/>
                <Rectangle Grid.Row="1" Height="1" Fill="#444444" Margin="0,0,0,20"/>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image x:Name="DialogIcon" Grid.Column="0" Width="80" Height="80" VerticalAlignment="Top" Margin="0,0,20,0" Stretch="Uniform" Visibility="Collapsed"/>
                    <TextBlock x:Name="MessageText" Grid.Column="1" FontSize="14" TextWrapping="Wrap" Foreground="#DDDDDD" Text="Message..."/>
                </Grid>

                <StackPanel x:Name="InputPanel" Grid.Row="3" Margin="0,20,0,0" Visibility="Collapsed">
                    
                    <StackPanel x:Name="Input1Container" Visibility="Collapsed">
                        <TextBlock x:Name="Input1Label" Foreground="#DDDDDD" FontSize="13" Margin="0,0,0,5" Text="Input 1" />
                        <TextBox x:Name="Input1Box" Background="#333333" Foreground="White" BorderBrush="#555555" BorderThickness="1" Padding="6" Margin="0,0,0,15" FontSize="14"/>
                    </StackPanel>

                    <StackPanel x:Name="Input2Container" Visibility="Collapsed">
                        <TextBlock x:Name="Input2Label" Foreground="#DDDDDD" FontSize="13" Margin="0,0,0,5" Text="Input 2" />
                        <TextBox x:Name="Input2Box" Background="#333333" Foreground="White" BorderBrush="#555555" BorderThickness="1" Padding="6" FontSize="14"/>
                    </StackPanel>

                </StackPanel>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="Button2" Click="Button2_Click" Content="Later" Width="100" Height="32" Background="#444444" Foreground="White" FontWeight="SemiBold" Margin="0,0,10,0" Visibility="Collapsed">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                    </Button>
                    <Button x:Name="Button1" Click="Button1_Click" Content="Reboot Now" Width="120" Height="32" Background="#0078D4" Foreground="White" FontWeight="SemiBold">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
